package com.shilpakala.ui.theme

import androidx.compose.ui.graphics.Color

// ── Primary Brand Colors ──────────────────────────────
val Terracotta = Color(0xFFC0522A)       // Deep saffron/terracotta - primary
val TerracottaLight = Color(0xFFD4754E)  // Lighter terracotta - buttons hover
val TerracottaDark = Color(0xFF8B3A1C)   // Darker terracotta - pressed state

// ── Secondary / Accent ───────────────────────────────
val HeritagGold = Color(0xFFD4A017)      // Gold/amber - accents, highlights
val HeritageGoldLight = Color(0xFFE8C04A)
val HeritageGoldDark = Color(0xFF9E7610)

// ── Background & Surface ─────────────────────────────
val WarmCream = Color(0xFFFFF8F0)        // Main background
val SoftParchment = Color(0xFFF5ECD7)   // Card/surface background
val LightSand = Color(0xFFEDE0C8)       // Dividers, borders

// ── Text Colors ───────────────────────────────────────
val DeepBrown = Color(0xFF3E1F00)        // Primary text
val MediumBrown = Color(0xFF6B3E1A)      // Secondary text
val LightBrown = Color(0xFF9E6B3E)       // Hint/placeholder text

// ── Status Colors ────────────────────────────────────
val SuccessGreen = Color(0xFF4CAF50)
val ErrorRed = Color(0xFFB00020)
val WhiteClean = Color(0xFFFFFFFF)
val BlackSoft = Color(0xFF1A1A1A)